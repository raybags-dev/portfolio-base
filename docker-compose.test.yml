# Test environment overlay: separate published port, debug logging on.
# Used as:  docker compose -f docker-compose.yml -f docker-compose.test.yml ...
services:
  backend:
    environment:
      ENVIRONMENT: test
      DEBUG: "true"
  nginx:
    ports:
      - "${HTTP_PORT:-8080}:80"
