-r requirements.txt

# --- testing ---
pytest==9.0.3
pytest-asyncio==0.25.0
pytest-cov==6.0.0
httpx==0.28.1

# --- quality ---
# Pinned to match local; must know all selectors used in pyproject.toml
# (e.g. UP046), otherwise CI fails with "Unknown rule selector".
ruff==0.15.16
mypy==1.14.0
