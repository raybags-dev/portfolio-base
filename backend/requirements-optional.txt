# ----------------------------------------------------------------------------
# Optional / heavy dependencies, grouped by feature-flagged module.
# Install only what a deployed node actually runs, e.g.:
#     pip install -r requirements.txt -r requirements-optional.txt
# or cherry-pick a group. Each block maps to feature flags in the admin panel.
# ----------------------------------------------------------------------------

# --- task queue / scheduling (ENABLE_SCHEDULER, ENABLE_RABBITMQ) ---
celery==5.4.0
APScheduler==3.11.0
croniter==5.0.1          # cron expression support for the scheduler
aio-pika==9.5.4          # RabbitMQ async client

# --- event streaming (ENABLE_KAFKA) ---
aiokafka==0.12.0

# --- crawlers / scraping (ENABLE_CRAWLERS) ---
beautifulsoup4==4.12.3
scrapy==2.16.0
playwright==1.49.1
lxml==5.3.0

# --- analytics / data engineering (ENABLE_PIPELINES, ENABLE_ANALYTICS) ---
polars==1.17.1
duckdb==1.1.3
pyarrow==18.1.0
pandas==2.2.3
great-expectations==1.2.4
dbt-core==1.9.1
dbt-postgres==1.9.0

# --- AI / agents (ENABLE_AI, ENABLE_AGENTIC_AI) ---
openai==1.58.1
langgraph==0.2.60
langchain==0.3.13
langchain-openai==0.2.14
tiktoken==0.8.0

# --- reports (ENABLE_REPORT_GENERATOR) ---
reportlab==4.2.5
jinja2==3.1.5
openpyxl==3.1.5
markdown==3.7

# --- storage (ENABLE_S3, ENABLE_STORAGE) ---
boto3==1.35.90
minio==7.2.13
